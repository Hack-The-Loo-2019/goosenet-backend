const addBuilding = require('./add_building');
const addUnit = require('./add_unit');
const generateSubscriptionCode = require('./generate_subscription_code');
const authenticateManagement = require('./authenticate');

module.exports = {
    addBuilding,
    addUnit,
    generateSubscriptionCode,
    authenticateManagement,
};
